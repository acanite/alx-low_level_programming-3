#include "main.h"

/**
 * print_number -checkk main
 * @n: integer params
 * Return: 0
 */

void print_number(int n)
{
	unsigned int ni;

	ni = n;

	if (n < 0)
	{
		_putchar('-');
		ni = -n;
	}
	if (n1 / 10 != 0)
	{
		print_number(n1 / 10);
	}
	_putchar((n1 % 10) + '0');
}
